# AgentCoolie Backend

The FastAPI service for AgentCoolie. Runtime architecture, required environment variables, SQL setup, and deployment guidance are documented in the repository root `README.md`.

Use `requirements.txt` for the Railway/Docker runtime and keep secrets in Railway variables or Supabase runtime secrets, never in this directory.
